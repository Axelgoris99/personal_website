test.goris.live {
    respond "it works"
}

goris.live {
    root * /home/debian/goris.live
    file_server
    encode gzip zstd
    try_files {path} /index.html

    log {
        output file /var/log/caddy/access.log
    }
}
